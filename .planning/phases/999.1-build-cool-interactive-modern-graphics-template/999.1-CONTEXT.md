# Phase 999.1 Context

## Goal
Create a highly interactive, modern, stunning 3D/AI-level portfolio with impressive graphics to replace the simple bootstrap template, utilizing cutting-edge web design techniques to stand out to elite clients and companies.

## Implementation Decisions

### Tech Stack
- **Locked Decision:** Pure Vanilla HTML, CSS, and JS. 
- **Rationale:** The user has explicitly mandated avoiding heavy build steps (like React/Next.js/Vite). To achieve the "AI-level" graphics, we will directly import CDN libraries such as **GSAP** (for pristine scroll animations/timelines) and **Three.js** (for 3D canvas rendering and particles) inside `main.js`.

### Visual Paradigm (Auto-Selected)
- **Locked Decision:** High-Tech "AI & Developer" dark mode aesthetic.
- **Rationale:** Deep, dark backgrounds `#050510` paired with glowing cyan/magenta accents. We will heavily utilize glassmorphism (frosted backgrounds), glowing grid backgrounds, and a hero section featuring a constantly moving, 3D interactive particle network (nodes resembling a neural network).

### Interactivity Depth (Auto-Selected)
- **Locked Decision:** Cinematic Scroll-Telling & Mouse Reactivity.
- **Rationale:** Combining massive staggered scroll reveals via GSAP ScrollTrigger with mouse-trailing effects. Cards will tilt dynamically when hovered, and the background canvas neural network will react to cursor position.

---
*This context file guides downstream planning and execution. The stack and visual direction are logged here so the planner agent knows exactly what to design.*
